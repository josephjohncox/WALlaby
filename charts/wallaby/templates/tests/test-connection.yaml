apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wallaby.fullname" . }}-test"
  labels:
    app.kubernetes.io/name: {{ include "wallaby.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: grpc-check
      image: busybox:1.36
      command: ["/bin/sh", "-c"]
      args:
        - |-
          echo "Checking port {{ .Values.service.port }}";
          nc -z -w 5 {{ include "wallaby.fullname" . }} {{ .Values.service.port }}
