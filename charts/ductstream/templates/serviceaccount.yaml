{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ductstream.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "ductstream.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
