WALLABY_POSTGRES_DSN=postgres://user:pass@localhost:5432/wallaby?sslmode=disable
WALLABY_GRPC_LISTEN=:8080
WALLABY_WIRE_FORMAT=arrow
WALLABY_WIRE_ENFORCE=true

# Run a single flow in its own process:
# ./bin/wallaby-worker -flow-id "flow-1"
