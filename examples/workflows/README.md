# Workflow Examples

This directory contains minimal configuration templates for each workflow engine:

- `cli.env` — local worker mode (`wallaby-worker`).
- `dbos.env` — DBOS scheduling configuration.
- `k8s.env` — Kubernetes job dispatch configuration.

Copy the file you need, update values, and export the variables before running WALlaby.
